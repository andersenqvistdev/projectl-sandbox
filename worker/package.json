{
  "name": "forge-playbook-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: verify-entitlement-then-serve gate for paid Forge Playbook content.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
