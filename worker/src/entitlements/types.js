// Extensible entitlement type enum. G3 implements PURCHASE; membership
// entitlements land later without changing the verify-then-serve gate shape.
export const EntitlementType = Object.freeze({
  PURCHASE: "purchase",
});
